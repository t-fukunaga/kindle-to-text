# Example .env configuration file
# Copy this to .env and customize as needed

# Output folder where captured images and PDFs will be saved
OUTPUT_FOLDER=~/Desktop/KindlePDF

# Key to press for turning pages (right or left)
PAGE_TURN_KEY=right

# Delay between captures in seconds (increase if pages are being skipped)
CAPTURE_DELAY=0.2